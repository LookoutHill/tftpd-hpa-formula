# -*- coding: utf-8 -*-
# vim: ft=yaml
---
tftpd-hpa:
  pkg:
    name: tftpd-hpa
  rootgroup: root
  config: '/etc/tftpd-hpa'
  service:
    name: tftpd-hpa
  subcomponent:
    config: '/etc/tftpd-hpa-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
